# iot-eslint-config-vue2

## 使用方法

1. 安装该配置文件

`npm install iot-eslint-config-vue2`

2. 在项目的`.eslintrc.js` 的 `extends`中添加配置

`.eslintrc.js`
```js
extends: [
    ...
    'iot-eslint-config-vue2'
]
```
